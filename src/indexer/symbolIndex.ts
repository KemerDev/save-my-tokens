import type { CodebaseIndex, SymbolMatch, SymbolQuery } from './types.js';
import { lexicalScore } from '../utils/text.js';
export function findSymbols(index: CodebaseIndex, query: SymbolQuery): SymbolMatch[] { const q = query.symbol.toLowerCase(); const rows = index.symbols.filter(s => (!query.kind || s.kind === query.kind) && (!query.fileHint || s.path.includes(query.fileHint)) && (!query.scope || s.path.includes(query.scope) || s.qualifiedName.includes(query.scope))).map(s => { let score = 0; if (s.qualifiedName.toLowerCase() === q) score += 1; if (s.name.toLowerCase() === q) score += .8; if (s.qualifiedName.toLowerCase().endsWith(`.${q}`)) score += .6; score += lexicalScore(q, `${s.qualifiedName} ${s.path}`) * .3; if (query.fileHint && s.path.includes(query.fileHint)) score += .3; if (s.exportStatus !== 'local') score += .1; return { ...s, confidence: Math.min(1, score) }; }).filter(s => s.confidence > 0).sort((a,b) => b.confidence - a.confidence || a.path.localeCompare(b.path)); return rows.slice(0, query.maxResults); }
export function resolveDependencyTargets(index: CodebaseIndex): CodebaseIndex { const byName = new Map(index.symbols.map(s => [s.name, s])); for (const d of index.dependencies) { const clean = d.reference.split('.').pop()?.replace(/\(.*/, '') ?? d.reference; const hit = byName.get(clean); if (hit) { d.toSymbolId = hit.id; d.resolution = 'resolved'; } else if (!d.reference.startsWith('this.')) d.resolution = 'external'; } return index; }

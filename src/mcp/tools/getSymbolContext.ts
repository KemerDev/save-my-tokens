import type { RuntimeContext } from '../../server.js';
import { getSymbolContextInputSchema } from '../schemas.js';
import { catchTool, common, resolveOne } from './common.js';
import { validateDependencyDepth } from '../../guardrails/depth.js';
import { snippetForRange } from '../../retrieval/contextPlanner.js';
import { recommendSnippet, recommendSymbolContext } from '../../retrieval/recommendations.js';
import { toolError } from '../../utils/errors.js';
export function getSymbolContext(context: RuntimeContext, raw: unknown) { return catchTool(async () => { const input = getSymbolContextInputSchema.parse(raw); const depth = validateDependencyDepth(input.dependency_depth, context.config); if (!depth.allowed) return toolError('DEPENDENCY_DEPTH_TOO_HIGH', depth.message!, { dependency_depth: input.dependency_depth }); const resolved = resolveOne(context, input.symbol, input.file_hint); if ('error' in resolved) return resolved.error; const include = input.include ?? {}; const body = include.body ?? context.config.includeCodeByDefault; const snippet = body ? await snippetForRange(context, resolved.path, resolved.range) : null; const deps = context.index.dependencies.filter(d => d.fromSymbolId === resolved.id); const imports = context.index.imports.filter(i => i.path === resolved.path); const included = input.dependency_depth > 0 ? deps.slice(0, 12).map(d => ({ symbol: d.reference, reason: `${d.relationship} dependency`, location: d.location, code: null, summary: `${d.relationship} ${d.reference}`, importance: d.importance })) : []; const parts = [resolved.signature ?? resolved.qualifiedName, snippet?.code ?? '', ...included.map(d => d.summary)]; return { ...common(input.max_tokens, parts, [recommendSnippet(resolved.path, resolved.range.startLine, resolved.range.endLine)], 'medium'), primary_symbol: { symbol: resolved.qualifiedName, kind: resolved.kind, signature: resolved.signature, location: { path: resolved.path, range: resolved.range }, code: snippet?.code ?? null, summary: `${resolved.kind} ${resolved.qualifiedName}` }, self_references: deps.filter(d => d.reference.startsWith('this.')).map(d => ({ reference: d.reference, assigned_from: null, assignment_location: d.location, assignment_code: null, type_guess: null, methods_called: [], importance: d.importance, included: input.dependency_depth > 0 })), this_references: deps.filter(d => d.reference.startsWith('this.')).map(d => ({ reference: d.reference, assigned_from: null, assignment_location: d.location, assignment_code: null, type_guess: null, methods_called: [], importance: d.importance, included: input.dependency_depth > 0 })), included_dependencies: included, external_dependencies: deps.filter(d => d.resolution === 'external').map(d => ({ symbol: d.reference, reason: 'external or unresolved dependency', location: d.location, code: null, summary: null })), imports, omitted_dependencies: deps.slice(included.length).map(d => ({ symbol: d.reference, reason: 'omitted due to depth or budget', suggestedCall: recommendSymbolContext(resolved) })) }; }); }
